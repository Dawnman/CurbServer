<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="us.curb.maven_.dao.QuestionKeyDao">
	<select id="querryBySdid" resultType="QuestionKey">
		select * 
		from question
		where sd_id=#{sdid};
	</select>
	
	
</mapper>